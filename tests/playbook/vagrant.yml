---
- hosts: all
  become: yes
  become_user: root
  roles:
  - role: ../../prometheus
    prometheus_external_labels:
      env: orange

    prometheus_target_jobs:
      - job_name: 'node-exporter.{{ domain }}'
        port: 9100
        targets_group: all 

